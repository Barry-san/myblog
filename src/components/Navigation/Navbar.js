import Header from "./Header";
import MobileNav from "./MobileNav";

const Navbar = () => {
  return (
    <div className="Navbar">
      <Header />
      <MobileNav />
    </div>
  );
};

export default Navbar;
